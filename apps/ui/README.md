# `apps/ui`

Tauri 2 + React + TypeScript UI. Tray app, dashboard, visualizations (`visx` + `react-simple-maps`). Read-only access to the SQLite DB written by the daemon.

Implementation begins in issue **#7 — Tauri 2 shell + tray icon + onboarding flow** (issue numbering subject to actual seed order).

This directory is intentionally empty until that issue is picked up.
