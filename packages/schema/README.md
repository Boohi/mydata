# `packages/schema`

Shared SQLite schema (single source of truth in `migrations/`), with codegen TypeScript types and Swift structs. Also hosts the XPC IPC message schema (`ipc.md`).

Implementation begins in issue **#6 — SQLite schema, writer, and retention sweeper**.

This directory is intentionally empty until that issue is picked up.
