# `packages/geoip-data`

Bundled DB-IP Lite GeoIP + ASN databases (CC-BY-4.0) and the update script that refreshes them at build time. The app makes no runtime GeoIP fetches.

Implementation begins in issue **#7 — Enrichment: GeoIP country, ASN, process attribution, DNS-flow join**.

This directory is intentionally empty until that issue is picked up.
