# Preserved project runtime

Finite source files and dependencies are pinned in provenance.json. These files
are project-owned; update them only after a reviewed source comparison and
relevant hook/plugin behavior checks. Existing optional providers stay separate.
